// ========== DOSYA: sentinel-spec/proto/sentinel/intelligence/v1/intelligence.proto ==========
syntax = "proto3";
package sentinel.intelligence.v1; // v1 eklendi

service SentimentAnalyzer {
  // Metni gönder, skoru al
  rpc AnalyzeText(SentimentRequest) returns (SentimentResponse);
}

message SentimentRequest {
  string text = 1;
}

message SentimentResponse {
  double score = 1; // -1.0 ile 1.0 arası
}
